# Dependencies for the experiments that need a GPU, on top of requirements.txt.
# Requires CUDA drivers.
#
#   pip install -r env/requirements.txt -r env/requirements-gpu.txt
#

-r requirements.txt

# Video classification and fault tolerance (Figures 7b, 7c)
torch==2.4.0
torchvision==0.19.0
transformers==4.44.2
decord==0.6.0

# ResNet-50 training and its tf.data baseline (Figure 8a)
tensorflow==2.16.1

# Retrieval-augmented generation (Figure 7a).
# vLLM pins its own torch; install it separately if pip reports a conflict.
vllm==0.6.3
faiss-cpu==1.8.0
datasets==2.21.0

# Spark baseline (Figures 7b, 9).
# Flink needs a JVM; use scripts/setup_baselines.sh.
pyspark==3.5.1
